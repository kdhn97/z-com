import LoginModal from "@/app/(beforeLogin)/_component/LoginModal";

export default function Page() {
  return (
    <LoginModal />
  );
}

// 브라우저로 직접 접근하거나, 
// 주소를 입력해서 접근하거나,
// 새로고침 했을 경우에 실행되고
// 아니면 (.)i/flow/login 실행